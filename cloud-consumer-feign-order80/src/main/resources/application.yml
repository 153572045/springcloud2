server:
  port: 80


eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:7001/eureka

spring:
  cloud:
    openfeign:
      client:
        config:
          product-service:
            connect-timeout: 1000
